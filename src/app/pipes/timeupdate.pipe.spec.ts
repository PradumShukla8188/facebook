import { TimeupdatePipe } from './timeupdate.pipe';

describe('TimeupdatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeupdatePipe();
    expect(pipe).toBeTruthy();
  });
});
