import { TextareaDirectiveDirective } from './textarea-directive.directive';

describe('TextareaDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
